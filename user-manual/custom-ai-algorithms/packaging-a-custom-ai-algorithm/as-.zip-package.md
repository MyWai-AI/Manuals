# As .zip Package

If your algorithm only exploits the mywai\_python\_backend library and its dependencies, your algorithm can be packaged and uploaded as a simple zip.

> Using this packing and deployment mode, any customised dependencies specified within your workspace will be **ignored**. If your code has custom dependencies, follow the directions for packaging using DockerImage.

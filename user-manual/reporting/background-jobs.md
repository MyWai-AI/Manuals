# Background Jobs

The **Background Jobs** page shows all asynchronous operations executed by the system, such as imports, deletions, and mass labelling. These jobs run in the background and may take time to complete depending on data size and system load.

When available, the information icon opens execution details such as logs or errors.
